import type { Meta, StoryObj } from '@storybook/angular';
import { ZoomLoopComponents } from './zoom-loop-components';

const meta: Meta<ZoomLoopComponents> = {
  title: 'Components/ZoomLoopComponents',
  component: ZoomLoopComponents,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<ZoomLoopComponents>;

export const Default: Story = {};
